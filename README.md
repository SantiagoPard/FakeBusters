# Para instalar el entorno se puede mediante el siguiente codigo

- conda create --name newenv --file requirements-conda.txt

# O si se quiere por el enviroment.yml esta
- conda env create -f environment.yml
